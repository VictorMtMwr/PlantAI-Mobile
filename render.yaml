services:
  - type: web
    name: plantai-mobile
    env: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    plan: free
    # Asegurar que se construya correctamente
    autoDeploy: true

